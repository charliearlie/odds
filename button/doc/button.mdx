import { mdx } from '@mdx-js/react';
import { Button } from '../index';
import { Props, Description } from '@divriots/dockit-react/props';
import { Playground } from '@divriots/dockit-react/playground';
import { Layout } from '~/mdx-layout';
export default Layout;

# Button

<Description of={Button} />

## Usage

```tsx
import { Button } from '@divriots/starter-react-aria/button';

<Button onPress={() => alert('Button pressed!')}>Test</Button>;
```

## Example

<Playground
  scope={{ Button }}
  code={`
<Button size="small" onClick={() => alert('small button pressed!')}>Small</Button>
<Button size="medium" onClick={() => alert('medium button pressed!')}>Medium</Button>
<Button size="large" onClick={() => alert('large button pressed!')}>Large</Button>
<br />

<Button onPress={() => alert('small button pressed!')}>Primary</Button>
<Button variant="secondary" onClick={() => alert('medium button pressed!')}>
  Secondary
</Button>
<Button variant="outline" onClick={() => alert('outline button pressed!')}>
  Outline
</Button>
<Button disabled onClick={() => alert('large button pressed!')}>
  Disabled
</Button>
`} />

## Props

<Props of={Button} />
