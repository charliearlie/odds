import { mdx } from '@mdx-js/react';
import { Button } from '../index';
import { Props, Description } from '@divriots/dockit-react/props';
import { Playground } from '@divriots/dockit-react/playground';
import { Layout } from '~/layout/mdx-layout';
export default Layout;

# Button

<Description of={Button} />

## Usage

```tsx
import { Button } from '@fpsm/odds/button';

<Button onClick={() => alert('Button pressed!')}>Test</Button>;
```

## Example

<Playground
  scope={{ Button }}
  code={`
<Button size="sm" onClick={() => alert('small button pressed!')}>Small</Button>
<Button onClick={() => alert('medium button pressed!')}>Medium</Button>
<Button size="lg" onClick={() => alert('large button pressed!')}>Large</Button>
<br />

<Button onClick={() => alert('Primary button pressed!')}>Primary</Button>
<Button variant="secondary" onClick={() => alert('Secondary button pressed!')}>
  Secondary
</Button>
<Button variant="outline" onClick={() => alert('outline button pressed!')}>
  Outline
</Button>
<Button disabled onClick={() => alert('I should never be seen')}>
  Disabled
</Button>
`} />

## Props

<Props of={Button} />
